# 2026-09-09

## 桌面清理脚本改目标
- 重写 `C:\Users\yile\Desktop\清理Yue后台并删除桌面exe.bat`：原为清理 `Yue launcher.exe` / `shortcut-launcher.exe`，现改为清理本项目的 `tauri-multi-account-browser.exe`（进程 + 桌面便携 exe），兼容 productName `Multi Account Browser.exe`。
- 脚本内容保持纯 ASCII（与原文件一致），避免 cmd GBK 代码页乱码；未执行该脚本。
- 已知：桌面存在 `tauri-multi-account-browser.exe`，运行时会被 `/T` 连同 WebView2 子进程一起杀掉。
- 待确认：脚本文件名仍含 "Yue"，用户尚未决定是否重命名。

## 导出关键文件供外部 AI 修改
- 需求：把项目关键文件提取到 Downloads，发给别的 AI 修改。
- 产出：`C:\Users\yile\Downloads\tauri-browser-关键文件-2026-09-09\`，含 23 个源文件（3877 行）+ `00_全部源码_合并.md`（142KB，单文件可直接粘贴给 AI）+ `00_导出说明.md` + 原始 README.md。
- 导出脚本保留在 `.workbuddy/export-key-files.mjs`（Node ESM，改 FILES 数组即可复用；注意模板字符串里不能出现反引号）。
- 未导出：node_modules、src-tauri/target、dist、Cargo.lock、package-lock.json、icons、src-tauri/gen。
- 用户工作流偏好：倾向于把代码整体交给另一个 AI 改，再覆盖回本地 —— 修改前提醒先 git 提交或备份。
