# v0.2.0 优化记录与下一版本建议

## 本版已完成

1. 下载历史增加多选、全选、分组级选择，选择状态支持跨分页保留。
2. 增加批量移动和批量删除；失败下载或无文件路径的记录也能单独从历史中移除。
3. 下载历史增加搜索、成功/失败筛选、分组/列表视图、10/20/50/100 行分页。
4. 下载历史保留上限改为可配置，默认 200 条，可选 100/200/500/1000；Rust 端再做 50~2000 的保护性限制。
5. 新增全局“更多设置”对话框，并在地址栏提供固定入口。
6. 新增 10 套主题：Claude 浅色、Claude 深色、午夜蓝、石墨黑、玻璃蓝、纸张奶油、紫晶、Win11、日落橙紫、薄荷浅色。
7. 新增舒适/紧凑界面密度、90/100/110% 字号、动画开关，设置通过 localStorage 持久化。
8. 将主 UI 大量硬编码颜色重构为 CSS 变量，主题只需维护 token，不再复制整套组件 CSS。
9. 修复右侧“下载设置” Popover 未抑制 child WebView 的层叠问题。
10. 增加后端 remove_download_history_entries 命令，支持失败记录、单条历史移除和批量“仅移除记录”。

## 代码结构观察

目前功能可以继续增长，但 `src-tauri/src/lib.rs` 已经承担 Profile CRUD、WebView 生命周期、状态抓取、下载、代理检测、favicon、注入脚本等过多职责。前端 `App.vue` 也同时负责 Profile、Tab、状态、下载和 WebView IPC 编排。下一轮更值得做的是“拆模块”，而不是继续把逻辑堆在单文件里。

建议 Rust 拆成：

- `profiles.rs`：Profile CRUD、排序、分组、持久化。
- `webviews.rs`：创建/销毁/切换/导航/边界同步。
- `downloads.rs`：下载策略、历史、移动/删除、批处理。
- `status.rs`：账号状态缓存、事件和 answer-ready。
- `network.rs`：代理检测、favicon 拉取。
- `scripts/` 或 `injection.rs`：长 JavaScript 注入脚本集中管理。

建议 Vue 拆成 composables：

- `useProfiles()`：账号加载、创建、修改、删除、排序。
- `useTabs()`：打开/关闭/激活与竞态控制。
- `useDownloads()`：历史加载、事件、设置与批量操作。
- `useUiPreferences()`：主题、密度、字号和持久化。

## 优先级较高的进一步优化

- **状态持久化节流**：当前账号状态更新会较频繁地 `store.save()`，账号数量多时会产生不必要磁盘写入。建议内存缓存 + 500~1500ms 合并落盘。
- **IPC 批处理**：拖动账号到分组目前会连续执行 update + reorder 两次 store 写入；建议后端提供原子 `move_profile` 命令。
- **下载批处理下沉 Rust**：当前批量移动/删除在前端循环调用 IPC，几十条以上时可以改成一个 Rust 批处理命令并返回逐条结果。
- **安全策略**：主窗口 `csp: null` 方便开发但长期不理想。建议为本地 UI 配严格 CSP，并把远程站点能力与主 UI 权限继续隔离。
- **错误边界**：`loadProfiles()` 等关键初始化调用应统一做错误提示和恢复态，避免 IPC 失败时空白但用户不知道原因。
- **日志系统**：增加可导出的结构化日志（不记录 Cookie/密码/网页正文），方便定位 WebView 创建失败、代理错误、下载失败。
- **无障碍与键盘操作**：给标签、下载列表、账号菜单补齐键盘焦点顺序、Esc、Enter、快捷键。

## 建议下一个版本：v0.3 “效率与稳定性”

最值得做的不是继续加颜色，而是做四个用户每天能明显感受到的能力：

1. **启动恢复**：记住上次打开的标签和激活账号，可选择“仅恢复最后一个”或“恢复全部”。
2. **标签页休眠**：后台账号超过一定时间自动休眠/销毁 WebView，需要时恢复，显著降低多账号内存占用。
3. **快捷键中心**：Ctrl/Cmd+K 搜账号、Ctrl/Cmd+J 下载历史、Ctrl/Cmd+, 设置、Ctrl/Cmd+数字切账号，并允许自定义。
4. **账号批量导入/导出**：只导出元数据与隔离配置，不导出密码/Cookie；支持 CSV/JSON，适合迁移和批量部署。

如果只选一个大功能，优先做“标签页休眠 + 启动恢复”，它对多账号浏览器的性能和日常体验提升最大。
