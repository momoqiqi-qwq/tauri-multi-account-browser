# v14 / 0.6.0

## 新功能

- 全局默认账号网址：在“更多设置 → 账号默认”中配置。
- 账号主页模式：每个账号可选择“继承全局”或“单独覆盖”。
- 旧账号自动兼容：升级后默认保持 custom，不会突然改掉既有主页。
- 修改全局默认网址时，仍停留在旧全局主页的继承账号会自动迁移；已经浏览到其他页面的 last_url 保留。
- 新增账号管理工具入口：账号模板、批量创建、JSON 导入/导出集中管理。
- 账号模板：可保存主页模式、网址、分组、代理、时区、语言、UA、指纹防护和临时账号设置。
- 批量创建：支持前缀、起始编号、数量、编号位数、模板与分组覆盖，并提供名称预览。
- JSON 导出：导出账号配置和模板（不导出 Cookie / 登录态）。
- JSON 导入：导入前预检查数量，以新增账号方式导入，不覆盖原账号 ID 和会话目录。
- 启动诊断中心：账号 WebView 打开失败时自动弹出。
- 一键修复：清除错误 last_url、回账号主页、切换到全局主页、禁用代理后重试、直接重试。

## 稳定性与兼容

- Rust 后端成为主页继承规则的唯一真值，避免前端传入旧默认网址造成状态漂移。
- `Profile.url_mode` 使用 serde 默认值兼容旧数据。
- `AppSettings.global_default_url` 使用 serde 默认值兼容旧设置。
- WebView 启动继续保留 last_url → 有效主页的兜底链路。
- 继承模式的“主页”按钮使用当前全局默认网址，而不是账号里历史缓存的网址。

## 验证

- 已通过全部 `src/**/*.ts` 与 Vue `<script setup lang="ts">` 的 TypeScript 语法转译检查。
- 已完成 Vue SFC 基础标签完整性扫描。
- 当前执行环境缺少完整项目 node_modules，`npm ci` 超时，因此未宣称完整 Vite/vue-tsc 构建通过。
- 当前执行环境无 Rust cargo/rustc，Rust 部分完成了结构、命令注册、参数和括号平衡静态核对；建议在本机 Tauri 开发环境执行 `npm install && npm run build && npm run tauri:build` 做最终编译验证。
