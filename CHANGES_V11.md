# v11 changes

- 标签页标题新增本次打开后的运行时长，按 `HH:MM:SS` 每秒更新；账号名过长时优先省略账号名，运行时长保持可见。
- 新增“页面打开后的下载保护”，默认 3 秒，可在“更多设置 -> 下载”中设置 0-60 秒，0 表示关闭。
- 下载保护采用双层实现：页面自动下载脚本在保护期结束前不点击，并在到期后重新扫描；Rust `on_download` 同时硬拦截保护期内的所有下载请求，覆盖手动点击和脚本触发。
- 页面导航开始与页面加载完成都会更新后端保护起点；页面加载后下发下载配置时同步重置前端保护起点，避免慢页面导致前后端计时漂移。
- 保护期内下载被阻止时，宿主界面显示明确提示。

## 验证

- 已执行源码结构与关键实现存在性检查。
- 当前环境没有 Rust toolchain，无法执行 `cargo check`。
- 项目依赖未完整安装，尝试 `npm ci --ignore-scripts` 超时，因此无法执行 `vue-tsc` / `vite build` 完整构建。
