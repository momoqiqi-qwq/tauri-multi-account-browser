# 本次修改

1. 下载历史每个文件新增“打开所在文件夹 / 移动 / 删除”三个操作。移动时输入目标文件夹；删除会同时删除文件本体和该条历史记录。
2. 下载设置新增“不下载已下载过的同名文件”。开启后，AI 自动下载会根据成功下载历史跳过同名文件，并在新下载完成后即时同步到所有已打开账号页。
3. 默认主窗口由 1280×820 调整为 1480×900，同时把左右侧栏从 252/196px 缩到 220/176px，给网站内容更多横向空间，便于显示“我的问题”等顶部导航。

说明：本环境缺少 Rust toolchain，且 npm 依赖安装超时，因此未能执行完整 cargo/npm 编译；已对改动文件做静态检查。

## 第二轮修改
- 标签页圆点不再使用按账号随机分配的颜色；统一为绿色，表示标签页已打开，避免黄色被误认为警告。
- 左侧账号支持跨分组拖动：拖到另一个账号或分组标题即可改变所属分组并保存顺序。
- 分组标题本身可拖动，从而调整整个分组的显示顺序。
- 左侧账号支持右键菜单：打开/切换、账号设置、检测代理、克隆、清除数据、删除。

## 第三轮修改
- 新建/编辑账号的“分组”改为可搜索下拉框：自动列出当前已有分组，同时保留直接输入新分组的能力。
- 多账号性能优化：窗口缩放和标签切换时只给当前可见 WebView 同步几何尺寸；后台账号显式进入低频状态扫描；状态监听不再观察 AI 流式输出的每个 `characterData` 变化，仅保留轻量 DOM 结构监听与需要时的下载属性监听。
- AI 回答状态提醒：检测到“停止生成/Stop generating”类生成状态后，后台账号回答结束会在“账号状态”积分行显示绿色“回答完成”；生成期间显示“回答中…”。切回该账号后自动清除完成提醒。
- 修复“刷新账号状态”自定义事件派发目标，使其与页面监听器保持一致。

验证说明：已用 Node `--check` 验证注入的 `STATUS_REPORTER_JS` 语法；当前环境没有 Rust toolchain，且项目随包的 `node_modules` 仅为空目录，`npm ci` 超时，因此无法完成 cargo / vue-tsc 全量构建。

## 第四轮修改：宿主快捷工具栏
- 将原来注入账号网页 DOM 的右下角刷新/主页按钮迁移到 Tauri/Vue 主界面，工具栏与账号子 WebView 使用独立布局区域，不再依赖远程网页刷新或脚本执行。
- 删除 `FLOATING_REFRESH_JS`、`mbaction://` 动作桥和 `profile:floating-*` 监听，减少注入代码、导航拦截和跨层事件链路。
- 新增宿主快捷工具栏：刷新、主页、后退、前进、复制网址、账号总览、下载历史、更多设置。
- 工具栏支持右侧/底部位置、紧凑模式、内置按钮开关与排序。
- 支持最多 12 个自定义网址按钮，可配置名称、Emoji/短字符图标、URL、启用状态与顺序。
- 修改工具栏布局设置后，Vue 完成重排即主动同步原生 WebView 边界，避免子 WebView 在位置切换瞬间盖住工具栏。
- “复制网址”增加兼容性回退；刷新按钮增加轻量加载反馈。
- 应用版本提升为 0.5.0。

验证说明：已对全部 `src/**/*.ts` 与 Vue `<script setup lang="ts">` 使用 TypeScript parser 做语法检查，并对全部 Vue `<template>` 做结构配对检查，均通过。当前运行环境缺少 Rust toolchain，随包 `node_modules` 不完整，`npm ci` 网络安装超时，因此无法执行完整 `cargo check` / `vue-tsc` 构建。
