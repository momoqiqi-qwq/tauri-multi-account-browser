# v12 changes

本版按需求加入 7 项能力：

1. **后台标签页自动休眠**
   - 默认开启，默认后台 15 分钟后休眠，可在“更多设置 -> 外观”中设置 1-1440 分钟。
   - 休眠会销毁该账号的后台 WebView，保留标签页、账号磁盘会话和 last_url；再次点击标签时自动重建并恢复。
   - 固定标签不会自动休眠。
   - 应用重启恢复多个标签时，只立即创建上次活跃标签，其余恢复为休眠标签，避免启动瞬间拉起所有 WebView。

2. **运行时间模式 + 暂停 / 重置**
   - 支持“本次打开”和“应用累计”两种模式。
   - “应用累计”在本次应用进程内保留账号累计运行秒数，即使关闭后再次打开标签也继续累计；应用重启后重新开始。
   - 休眠期间不会累计运行时间。
   - 标签页运行时间按钮可直接暂停/继续；旁边的重置按钮可清零当前账号运行时间。

3. **下载保护域名规则**
   - 保留全局默认保护时间（默认 3 秒）。
   - 新增最多 20 条域名规则，每个域名可设置 0-60 秒独立保护时间；0 秒相当于可信下载域名白名单。
   - 域名规则同时匹配其子域名。
   - Rust `on_download` 负责最终匹配和硬拦截。

4. **下载队列 + 自动重试**
   - 新增“保护期下载自动排队”开关，默认开启。
   - 同一下载 URL 在保护期内只加入一次队列，避免重复排队。
   - 到剩余保护时间结束后，由宿主重新触发一次下载，无需用户再次点击。
   - 前端提示会显示“已加入队列”和大致剩余秒数。

5. **标签页宽度**
   - 支持紧凑 / 标准 / 宽三档。
   - 通过设置即时影响标签最大宽度，多账号时可切换紧凑模式。

6. **标签页排序和固定**
   - 标签支持原生拖拽排序。
   - 固定标签自动集中在前部；拖动只允许在同类（固定/非固定）标签内调整，避免固定区被意外打乱。
   - 固定标签隐藏关闭按钮，中键关闭也会被阻止，必须先取消固定。
   - 固定状态和标签顺序写入本地会话存储。

7. **崩溃 / 重启恢复**
   - 持久化已打开标签、标签顺序、固定状态和上次活跃账号。
   - 启动后校验账号是否仍存在，自动清理失效 ID。
   - 恢复上次活跃账号；其他标签保持休眠，点开再唤醒。

## 验证情况

- 已检查新增 TypeScript / Vue / Rust 字段的前后端命名一致性。
- 已检查 AppSettings 对旧配置的 serde 默认兼容：旧版设置文件没有新字段时会自动使用默认值。
- 已检查新增 `hibernate_profile` 已注册到 Tauri command handler。
- 已检查下载保护事件前后端 payload 一致。
- 已检查标签恢复、固定、休眠、拖拽排序和运行时间事件已接入 `App.vue`。
- 当前容器没有 Rust toolchain，无法执行 `cargo check`。
- 当前 `node_modules` 不完整，`npm run build` 因 `vue-tsc` 缺失无法执行；直接 `tsc --noEmit` 也因缺少 `vite/client` 类型定义失败。因此本包属于源码级实现与静态核对，不宣称完成了最终平台编译验证。
