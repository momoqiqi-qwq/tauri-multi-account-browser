# v13 / 0.5.1 优化说明

本版重点修复“账号没有网址时无法进入”，并围绕账号打开、设置保存、启动速度与拖拽一致性做了一轮低风险体验优化。

## 1. 修复：空网址 / 坏网址导致账号无法进入

- 新增统一的默认账号主页常量：`https://chat01.ai/`。
- `Profile.default_url` / `Profile.last_url` 增加 serde 默认兼容，旧数据缺字段时不再直接反序列化失败。
- 加载账号时自动修复并持久化以下异常：
  - `default_url` 为空；
  - `last_url` 为空；
  - 已保存 URL 损坏或不是可用的 http(s) URL。
- WebView 创建时增加三层兜底：`last_url -> default_url -> 应用默认主页`。
- 新建账号时默认网址留空会自动配置应用默认主页。
- 编辑账号时把默认网址清空，会明确恢复应用默认主页，不再保留不可见旧值。
- 当账号仍停留在旧默认主页时，修改默认主页会同步更新 `last_url`，下次进入直接使用新配置。

## 2. 主页按钮改为账号自己的配置

- 之前“主页”固定打开 `https://chat01.ai/`。
- 现在优先打开当前账号的 `default_url`；只有该配置缺失/损坏时才回退应用默认主页。
- 设置页文案同步说明这一行为。

## 3. 网址校验统一

- 新增 `src/lib/profileUrl.ts`，前端账号设置、地址栏、账号恢复统一使用一套 URL 规则。
- 自动为无协议地址补 `https://`。
- 支持大小写形式的 `HTTP://` / `HTTPS://`。
- 明确拒绝 `ftp://` 等非 http(s) scheme。
- Rust 后端继续做第二次校验，避免绕过 UI 写入脏数据。

## 4. 设置保存体验

- 新建/编辑账号保存失败时不再立即关闭弹窗，用户已填写内容会保留。
- 保存按钮增加 loading，避免连续点击重复提交。
- 昵称增加前后端双重非空校验，后端限制最多 80 字符。
- 默认网址失焦时自动规范化；空值自动补默认主页。

## 5. 打开账号失败时不污染标签状态

- 以前前端先把标签标记为已打开，再请求 Rust 创建 WebView；创建失败后容易留下“标签存在但点不进去”的假状态。
- 现在先确认后端成功，再提交前端 `openTabs / activeId / runtime` 状态。
- 打开失败会显示账号名和错误信息，原来的活跃账号保持不变。

## 6. 拖拽分组一致性

- “修改分组 + 保存排序”从前端两次 command 改为 Rust `move_profile_to_group` 一次事务式落盘。
- 减少一次 store 读写和一次 IPC，也避免第二步失败后只改了分组却没保存排序。
- 分组拖拽取消时补充 drag state 清理。
- 折叠分组的 localStorage 数据损坏时安全回退为空集合，不再影响侧栏初始化。

## 7. 启动和空状态体验

- 账号列表加载完成后，状态、下载设置、下载历史改为 `Promise.all` 并行加载。
- 没有打开账号时，浏览区增加“新建账号”直接入口，不必先去左侧栏找按钮。

## 验证

已完成：

- 所有 `src/**/*.ts` 与 Vue `<script setup>` 的 TypeScript 语法级转译检查；
- Vue 顶层 `<script>/<template>` 结构检查；
- `profileUrl.ts` 行为测试：空 URL、无协议 URL、大写 HTTP、损坏 last_url、非法 scheme；
- Rust 新增 command 注册、URL 修复链路与前后端 command 名称静态核对。

当前容器没有 Rust toolchain，无法执行 `cargo check`。项目 `node_modules` 原本为空，尝试 `npm ci` 时环境安装进程未能在工具调用时限内完成，因此不宣称已经完成完整 `vue-tsc + vite + tauri build` 编译。
