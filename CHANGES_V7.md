# V7 changes

## 用户可见更新

- 顶部账号标签页接入 AI 状态：检测到当前账号正在生成回答时显示动态 `...` 和蓝色脉冲点；后台回答完成后显示“完成”。
- 地址栏右侧新增“全部账号状态”按钮。状态总览展示账号、打开状态 / AI 状态、积分、最近下载文件名称、最近下载时间、状态更新时间；点击任意一行直接切换到对应账号。
- 打开“全部账号状态”时自动触发一次状态抓取，也保留手动刷新按钮。
- 自动刷新新增“AI 回答时暂停自动刷新”选项，默认开启，防止生成过程中被刷新打断。
- “更多设置”新增状态显示自由度：标签页 AI 状态、登录账号、代理出口、更新时间均可独立开关。

## 下载白名单修正

- 扩展名白名单保存前在前端和 Rust 后端双重规范化：自动去除前导点、空白、统一小写、去重，并过滤异常扩展名。
- 白名单非空时，自动下载必须识别到扩展名且扩展名在白名单内；无扩展名 / 无法判断格式的链接不再自动放行。
- 设置保存后立即把规范化后的白名单下发到全部已打开账号页面。
- 保留“白名单为空 = 自动下载全部可识别下载链接”的既有行为。

## 代码与体验优化

- 新增独立 `AccountOverviewDialog.vue`，避免继续把 App.vue 膨胀成单体组件。
- 标签页状态展示复用既有 `ProfileStatus.answer_generating / answer_ready`，不增加第二套状态来源。
- 最近下载信息直接复用下载历史，在总览组件内一次性构建账号 -> 最近记录映射，避免每行重复扫描历史数组。
- 总览弹窗沿用 WebView suppression 机制，防止原生子 WebView 覆盖 Vue 弹层。

## 建议下一轮确认后再做的结构升级

1. 把 `App.vue` 中 profile / browser / download / status 的 IPC 和状态逻辑拆成 composables，降低单文件复杂度。
2. 为 Tauri command 增加统一的错误包装与前端 toast 策略，减少重复 catch。
3. 增加标签页休眠 / 最大活跃 WebView 数，账号很多时明显降低内存和 CPU。
4. 下载改成任务队列：显示进行中、失败原因、重试、取消和并发数设置。
5. 增加快捷键中心与可配置快捷键。
6. 增加账号状态排序 / 筛选（AI 使用中、回答完成、低积分、最近下载）。
