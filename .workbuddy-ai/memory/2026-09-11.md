# 2026-09-11

## 升级到 v14 / 0.6.0

从 `D:\Downloads\tauri-multi-account-browser-v14.zip` 覆盖升级工作区源码，版本从 0.5.0 → 0.6.0。

变更文件：`src/**`（含新增 `src/lib/profileUrl.ts`、`src/components/AccountToolsDialog.vue`、`src/components/ProfileDiagnosisDialog.vue`）、`src-tauri/src/lib.rs`、`Cargo.toml/Cargo.lock/tauri.conf.json`、`package.json`、README 与 CHANGES_V11~V14 文档。

升级前备份：`.workbuddy-ai/backup/pre-v14-20260911-150531.tar.gz`（不含 node_modules / target / dist）。

## 修复的问题（v14 包自带，官方包自述未做完整构建验证）

1. **`src/components/StatusSidebar.vue` 类型错误（原本阻塞 `npm run build`）**
   - `emit('saveSettings', {...})` 只传了 6 个字段，缺失 v11/v12/v14 新增的 `download_guard_seconds`、`download_guard_rules`、`download_guard_auto_retry`、`global_default_url`，vue-tsc 报 TS2769。
   - 更严重的隐患：Rust `set_app_settings` 是**整体覆盖写盘**，而 `skip_downloaded_files` 在 Rust 侧被强制置 `true`。所以侧栏保存下载设置会把这些字段重置为 serde 默认值（下载保护 3 秒、全局主页回落 chat01.ai），并触发继承账号 last_url 迁移 → 属于真实数据丢失 bug。
   - 修法：以 `props.dlSettings` 为基底透传这 4 个字段，只覆盖下载相关字段。

2. **`src-tauri/src/lib.rs` 移除死常量 `PROFILE_TEMPLATES_KEY`**
   - v14 声明了该 store key 但从未使用（模板实际存在前端 localStorage `mab-profile-templates-v14`），cargo check 报 `constant is never used`。已删除。
   - 注意：模板存 localStorage 是 v14 的既定设计，`OPTIMIZATION_REPORT_V14.md` 第 1 条建议后续迁入 Rust store。

3. **`format!(".{domain}")` 经核查不是 bug**
   - 这是合法的 Rust **内联格式参数捕获**（等价 `format!(".{}", domain)`），源文件与 v14 包逐字节一致，未改动。
   - 我早期的括号计数器报负数属于剥离字符串时的误报（`'/' | '_' | '-'` 被当成字符字面量），已用 `rustfmt --check` 证伪：解析 0 错误。

## 验证结论（全部通过）

- **`npm run build`**（vue-tsc --noEmit + vite build）通过，1629 模块，产物 `dist/`。
- **`cargo check --lib` 与全目标 check** 通过，**零警告零错误**。
- **`cargo build --release` 通过**（3m03s，LTO），产物 `src-tauri/target/release/tauri-multi-account-browser.exe`（4.81 MB，15:19 生成）。

## 关键环境解法（重要，可复用）

Git Bash 的 `/usr/bin/link.exe`（GNU hardlink 工具）会**遮蔽 MSVC 链接器**，导致 `tauri-build` 构建脚本报
`link.exe returned an unexpected error` / `LNK1181: 无法打开输入文件"kernel32.lib"`。

解法见 `scripts/cargo-msvc.sh`：
1. 把 `MSVC\...\bin\Hostx64\x64` 前置到 PATH，让 `link.exe` 解析到 MSVC 版本；
2. `INCLUDE` / `LIB` / `LIBPATH` 必须用 **Windows 反斜杠绝对路径**（bash 里反斜杠需转义或用单引号），传 POSIX 路径无效；
3. 用 bash 脚本封装环境，避免在 Bash 工具里多次 `export` 被引号处理破坏。

组件路径：VS 18 Community / MSVC 14.50.35717 / SDK 10.0.26100.0。

## 后续：模板迁入 Rust store（CHANGES_V15）

落实 `OPTIMIZATION_REPORT_V14.md` 第 1 条建议，模板从 localStorage 迁到 Rust store。

- 新增 `ProfileTemplate` 结构体 + `list_profile_templates` / `save_profile_templates` 命令，
  写入 `profiles.json` 的 `profile_templates_v1` 键（复用了 v14 遗留的常量，不再是死代码）。
- `normalized_templates()` 做规范化：trim、剔除无名/无 id、url_mode 校验、上限 200。
  `save` 命令返回规范化后的列表供前端回写，避免前后端状态漂移。
- 前端 `AccountToolsDialog.vue`：打开时 `loadTemplates()`（异步），
  store 为空且 localStorage `mab-profile-templates-v14` 有数据则一次性迁移并清空旧键（幂等）。
- 命名坑：命令名与内部 helper 不能同名。最初把 helper 也叫 `save_profile_templates` 与命令冲突，
  已把 helper 改名为 `store_profile_templates`。

验证：`npm run build` 通过、`cargo check` 零警告、`cargo build --release` 通过。
前后端命令交叉核对无遗漏，`ProfileTemplate` 11 个字段 diff 一致。

## 后续：批量创建改原子提交（CHANGES_V16）

落实 `OPTIMIZATION_REPORT_V14.md` 第 3 条。

- 新增 `create_profiles_bulk` 命令：**先整体校验、全部通过才 `save_profiles`**，
  任一条不合格则整批不落盘；错误带序号（`第 3 个账号：...`）；上限 200。
- 抽出 `ProfileDraft` + `build_profile()`，让单个创建与批量创建共用同一套校验，消除逻辑分叉。
- 前端 `createBatch` / `importJson` 改为单次 `invoke('create_profiles_bulk', { drafts })`，
  失败文案改为「未创建任何账号」。
- **新增 15 个 Rust 单元测试**（`#[cfg(test)] mod tests` 追加在 lib.rs 末尾）：
  覆盖昵称/URL/代理/时区语言注入防护/模板规范化/ProfileDraft 反序列化。
  跑法：`bash scripts/cargo-msvc.sh test --lib`。

验证：`cargo test --lib` 15 passed 0 failed、`npm run build` 通过、
`cargo check` 零警告、`cargo build --release` 通过。



## 20:00 重做 v15/v16/v17（v14 压缩包曾覆盖工作区）

### 事故
16:03 左右 v14 zip 被重新解压到工作区，`src/`、`src-tauri/src/lib.rs`、
`Cargo.toml` 全部还原成 v14 基线，v15/v16/v17 三批改动丢失。
幸存的：`scripts/cargo-msvc.sh`、`src-tauri/build.rs`、`src-tauri/tests/`、`CHANGES_V15/V16.md`。
**教训：该项目没有 git，改代码前必须先备份。** 已新建备份
`.workbuddy-ai/backup/v14-baseline-20260911-1955.tar.gz`。

### 重做内容（全部完成并验证）
1. `StatusSidebar.vue`：修回 `emit('saveSettings')` 漏传 4 个字段导致设置被静默重置的
   数据丢失 bug（Rust 侧是整体覆盖写盘）。
2. v15 模板迁 Rust store：`ProfileTemplate`(11 字段) +
   `load/store_profile_templates` + `normalized_templates` + 两个命令；
   `AccountToolsDialog.vue` 改 async invoke，localStorage 一次性幂等迁移。
3. v16 原子批量创建：`ProfileDraft` + `build_profile()`（单/批量共用校验）+
   `create_profiles_bulk`；前端 `createBatch`/`importJson` 改单次 invoke。
4. v17 schema_version：`SCHEMA_VERSION=1` + `stored_schema_version` +
   `migrations()` + `migrate_v0_to_v1` + `migrate_store`（超前版本拒绝且不改数据），
   在 `.setup()` 里调用；11 个函数泛化为 `<R: Runtime>`。

### 解决 Windows 测试二进制 0xc0000139
根因：`tauri` 的 `test` feature 链入 tray-icon/muda → `comctl32.dll!TaskDialogIndirect`，
只在 Common-Controls **v6**（WinSxS）里存在；`System32\comctl32.dll` 是 v5.82 不导出它。
正式 app 由 tauri-build 注入 manifest，测试 harness 没有 → 加载失败。

三个死胡同（已验证无效）：`build.rs` 拿不到 `CARGO_CFG_TEST`；
`cargo:rustc-link-arg-tests` 不覆盖 `--lib` harness；
无作用域 `rustc-link-arg` 会让 app 出现 CVT1100 资源重复。

最终方案：静态 manifest 文件 `src-tauri/common-controls.manifest` +
`scripts/cargo-test.sh` 用 `cargo --config ...rustflags=[...]` 只在测试时注入。
`mt.exe`（Windows SDK）也必须在 PATH，已加进 `cargo-msvc.sh`。

### 测试结果
`bash scripts/cargo-test.sh --lib` → **22 passed; 0 failed**。
另发现 store 在 mock app 间**进程内共享**，测试会互相污染；
已加 `reset_store()` 并强制 `RUST_TEST_THREADS=1`。

### 验证
- `npm run build`（vue-tsc + vite）通过，1629 模块。
- `cargo check --lib --bins` 零警告。
- `cargo build --release` 通过（3m36s），无 CVT1100。
- 前端 invoke 与后端命令交叉核对：无悬空调用。
- `ProfileTemplate` 前后端 11 字段 diff 一致。
